# 本轮验证记录

日期：2026-09-19。

## 实际执行

| 项目 | 结果 | 适用范围 |
|---|---|---|
| `python -m unittest discover -s tests -v` | **76项通过** | 本地合成数据、模拟模型接口及本地Git仓库 |
| `python -m compileall -q wxq_assistant tools tests` | 通过 | Python语法/字节码编译 |
| `python -m wxq_assistant demo` | 成功输出白歌进度、目标范围及缺资料提示 | 仅离线合成示例 |
| 旧局面使用 `--mode live` | 按预期拒绝，退出码2 | 不篡改旧观察时间 |
| 桌面启动、事件循环、关闭 | Linux/Xvfb烟雾测试通过 | 不等于Windows客户端采集或画面布局实测 |
| 上游导入与叠加流程 | 用本地真实Git仓库测试通过 | 验证原历史、基线main、开发分支、远端地址、暂存及冲突保护；未完成真实GitHub联网克隆 |

## 测试覆盖

字段与类型、未知值、时间时区、重复ID、重复JSON字段、NaN、对象变动指纹；能量不足、费用未知、阶段不符、无效对象、随机效果不能指定命中、目标集合不完整、觉醒资格未知；缺来源、虚构引用、错快照、非法建议撤换；新请求与新局面使旧请求失效；HTTP错误脱敏、网络超时、截断和过大响应、非JSON响应、HTTPS限制、拒绝HTTP重定向；视觉模型不能自动确认或更新采集时间；导入路径穿越、私密路径、哈希不符、目标冲突与原始README保留。

所有模型传输单元测试使用模拟响应，未使用真实API Key，也没有计入真实服务调用或服务质量验证。

## 未完成或未验证

- 目标远程仓库写入：实际调用返回 **403 / Resource not accessible by integration**。
- 最后复查目标 `/contents/` 仍返回 **This repository is empty**；没有成功提交、分支或PR。
- 工作容器DNS无法解析GitHub，不能在本轮完整克隆上游；本包明确为升级层，不伪装成含上游的整仓副本。
- Windows游戏窗口采集、DPI、遮挡、黑屏兼容性。
- 真实视觉识别准确率、真实大模型推导质量、端到端延迟与费用。
- 当前版本游戏机制的全面核验、完整多步计算与战斗模拟。
- 游戏官方对实际排位使用这种辅助的许可边界。

**结论：代码原型与局部校验可运行；不能据此宣称已交付到GitHub，或已经成为可靠的实时上分助手。**


## GitHub 接通后的本轮复验（2026-09-19）

- 当前集成已实际成功创建 README 和开发分支，之前的403不再发生。
- 76项 unittest 在工作环境重新运行，全部通过；compileall 与离线 demo 通过。
- 上游导入任务已成功，固定快照及全部原始 Git 历史保留在开发分支。
- 当前尚未在用户 Windows 游戏客户端实测采集，也未调用真实付费模型接口。
- GitHub 上本次升级的检验结果以对应 Actions 运行记录为准，不将本地测试等同于 Windows 实机验证。
